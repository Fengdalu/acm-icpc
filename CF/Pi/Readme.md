# CodeForce #314 div2

## A

计算每个点到旁边点和端点距离

## B

如果一个-出现但是+没有在之前出现，可以认为+出现在队列最开头，这样保证构造的日志使得lib人数最多

## C

对于每个c[i]，计算在1 <= j < i中i / k的个数记为a[i]，i < j <= n中i * k的个数记为b[i]，最后ans = sigma(a[i] * b[i])
a[i], b[i]可以用map，树状数组等结构优化

## D

二分操作数，对于长度为l的区间，必然可以容下(l + 1) / (a + 1)条船，线性检验即可