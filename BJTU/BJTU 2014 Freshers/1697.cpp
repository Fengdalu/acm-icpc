#include <iostream>
#include <cstdio>
#include <string>
#include <cstring>
using namespace std;

int x, g, h, k;
int buf[100];
int n;

int main()
{
	ios::sync_with_stdio(false);
	int T; cin >> T;
	for(int ii = 0; ii < T; ii++)
	{
		cin >> x >> b >> h >> k >> n;
		for(int i = 0; i < n; i++) cin >> buf[i];
		
	}
}