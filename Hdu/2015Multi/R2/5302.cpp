#include <iostream>
#include <cstdio>
#include <cstring>
#include <set>
#include <algorithm>
using namespace std;
int w[3], b[3];
int bg[20], ed[20];
int n;

int main()
{
    int T; scanf("%d", &T);
    while(T--)
    {
        for(int i = 0; i < 3; i++) scanf("%d", &w[i]);
        for(int i = 0; i < 3; i++) scanf("%d", &b[i]);

    }
}
