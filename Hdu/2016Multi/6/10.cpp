#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;

int main() {
    int _T; scanf("%d", &_T);
    while(_T--) {
        int p, q; scanf("%d%d", &p, &q);
        if(q >= p) printf("%d\n", q - p);
        else {

        }
    }
    return 0;
}
